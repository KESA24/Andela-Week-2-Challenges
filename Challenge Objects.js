//defining and assigning my variables
var object = x 
var output = "";
var result = "";
    for(var x= 1; x<=15; x++)
    {
        result = x * x;
        output = output + x + " "+ result +",";

    }
document.write (output);
